/**
 * 인증이 B4 라 지금은 워크스페이스와 사용자를 고를 방법이 없습니다.
 * 그동안 쓸 고정값을 여기 둡니다.
 *
 * env 로 빼지 않은 이유 — 환경마다 달라져야 하는 값이 아니라 B4 까지만
 * 사는 임시값입니다. 파일 하나에 모아두면 B4 에서 이 파일을 지우는 것으로
 * 임시 분기가 남김없이 사라집니다.
 */

// 시드가 만드는 워크스페이스 하나. B4 전까지 모든 페이지가 여기 속합니다
export const LOCAL_WORKSPACE_ID = '00000000-0000-4000-8000-000000000001';

// Page.createdBy · updatedBy 에 들어갈 값. B4 에서 User.id 를 가리키는 FK 가 됩니다
export const LOCAL_USER_ID = 'local-user';
