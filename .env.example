# 채워야 하는 키 목록입니다. 값은 여기 적지 않습니다.
# 이 파일을 .env.local 로 복사한 뒤 값을 채우세요.
#   cp .env.example .env.local
#
# DATABASE_URL  로컬은 docker compose 로 띄운 Postgres
# CORS_ORIGINS  쉼표로 구분. 프론트 dev 서버 주소
DATABASE_URL=
PORT=
NODE_ENV=
CORS_ORIGINS=
