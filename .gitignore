node_modules
# Keep environment variables out of version control
# .env.example 만 예외. 값 없이 키 이름만 들어 있습니다.
.env*
!.env.example

/src/generated/prisma

dist
*.log

# 에이전트 스킬 링크. 절대경로 심볼릭 링크라 다른 컴퓨터에서 깨집니다.
# 원본은 .agents/ 에 있고, 링크는 각 도구가 다시 만듭니다.
.claude/
.windsurf/
